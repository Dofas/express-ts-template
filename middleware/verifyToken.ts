import jwt from 'jsonwebtoken';

export default function verifyToken(token: string) {
    // try {
    //     if (token) {
    //         return jwt.verify(token, process.env.);
    //     }
    // } catch (e) {
    //     return null;
    // }
    return null;
}
